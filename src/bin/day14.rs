use advent_of_code::ex14::Day14;
use aoc_utils::DaySolver;

fn main() {
    Day14::timeit_solve_default_file();
}

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
